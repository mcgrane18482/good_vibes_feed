export default function Quotes() {
    return (
        <div className="quoteSection">
            <h2 className="quote">{quote}</h2>
            <h2 className="quote">{quote}</h2>
        </div>
    );
    );
}
